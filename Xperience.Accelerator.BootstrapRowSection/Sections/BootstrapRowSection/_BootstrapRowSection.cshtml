@using Kentico.Web.Mvc
@using Kentico.PageBuilder.Web.Mvc
@inject IPageBuilderDataContextRetriever pageBuilderContext

@model ComponentViewModel<Xperience.Accelerator.BootstrapRowSection.Components.Sections.BootstrapRowSection.BootstrapRowSectionProperties>

@if (pageBuilderContext.Retrieve().EditMode)
{
    Html.RenderPartial("~/Sections/BootstrapRowSection/_BootstrapRowSectionEditor.cshtml", Model);
}
else
{
    Html.RenderPartial("~/Sections/BootstrapRowSection/_BootstrapRowSectionDisplay.cshtml", Model);
}